# 2026-07-19 工作日志

## 修复：前端默认不走 AI 的问题（v4.3）

- **问题：** `index.html` 第 159 行表单提交时 `generateDraft(false)` 硬编码，导致首次生成走"快速模式"（确定性兜底），不调 LLM
- **根因：** v4.2 有意设计的"快速模式优先"策略（首屏秒出），但用户需要多一步"重新拆解"才能看到 AI 效果
- **修改：** `false` → `true`
- **验证：** 语法检查 OK | 无 Unicode 转义问题 | pytest 53 passed（9 个 pre-existing failure 全是 API Key 缺失，与本次修改无关）
- **CHANGELOG：** 新增 v4.3 条目，包含完整的前后代码对照和设计取舍说明

## AI 调整建议按钮可拖拽 + 生成按钮反馈修复（v4.4 完整修复）

- **拖拽冲突修复：** 移除 `btn.onclick=openAssistant`，改用 IIFE 内 `addEventListener('click',...)` 配合 `dragging` 标志条件拦截，拖拽松手不再弹出抽屉
- **按钮反馈修复：** `generateDraft(useAi, btn)` 增加可选 `btn` 参数，表单提交传入 `el('generateBtn')`，spinner 正确显示在用户点击的按钮上；`redraftBtn` 不传参自动退回到自身
- **CSS：** `cursor:grab` + `.dragging` 态 + `user-select:none` + `touch-action:none`
- **验证：** 语法检查 OK | pytest 53 passed
