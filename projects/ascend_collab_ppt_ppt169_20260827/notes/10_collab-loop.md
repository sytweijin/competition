本页展示协作闭环：计划生成后成员执行，成员通过语音、照片或文本汇报反馈，负责人确认完成，状态变更自动通过 Webhook 推送到企业微信、飞书或钉钉，并提供今日播报；每一次状态变更都回到即时重算，让计划随现实持续运转，而不是一次生成、永久过时。四项关键机制包括：token 免登录，成员只看自己的任务、手机即用；语音与照片汇报，语音上报状态与工时、拍照交付；Webhook 群通知自动推送；今日播报用云端 TTS 朗读今日待办要点。