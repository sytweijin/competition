本页给出五大能力模块的全景。计划引擎是核心：它把项目要求拆成有依赖关系的子任务，估算工时，按技能匹配出三层角色，用 CPM 算法排出关键路径并生成风险报告，支持小型与大型两种项目模式。动态协作保证计划实时更新：草案可以增删改、拆分合并，分工可以拖拽，成员变动、状态切换、工时变化都会触发即时重算，每次保存都生成版本快照，可以对比、可以回滚。全模态交互覆盖五种模态，每个输入都能变成计划的一部分。汇报与通知让成员用 token 链接免登录汇报，语音或照片都能交，负责人确认后变更自动推送到工作群。工程能力包括多格式导出、知识库问答、鉴权审计与演示前预检，保障可运行、可复现、可审计。