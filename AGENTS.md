# AGENTS.md — 项目约定

## 版本号管理（重要）

- **小 bug 修复、命名修正、配置调整不新增版本号**：直接并入当前版本的 CHANGELOG 条目（追加"同步修改"说明），版本号保持不变。
- **只有功能版本发布才递增版本号**（如新增完整功能、里程碑交付）。
- **静态资源缓存参数不跟随产品版本**：`index.html` 中 `app.js` / `participants.js` / `style.css` 的 `?v=` 使用文件内容哈希（sha1 前 8 位），文件改动后重新计算即可，避免版本号被当缓存号频繁迭代。

## CHANGELOG 写作规范（重要）

每次修改代码时必须同步更新 CHANGELOG.md，格式必须严格按照以下模板：

---

## vX.X —— 标题（YYYY-MM-DD）

**定位：** 一句话说明本版本核心目标。

**审查/修改背景（可选）：** 为什么会有这次修改。

---

### 每条修改的格式

每条修改必须包含以下 5 个部分：

1. **问题：** 一句话描述为什么这是问题。
2. **修改前：** 贴旧代码（标注问题所在行）。
3. **修改后：** 贴新代码（突出关键改动）。
4. **为什么这样改：** root cause 分析——旧写法的问题根源，新方案为什么更好。
5. **收益：** 这样改带来的具体好处（至少 1 条，最多 3 条）。

**同步修改（可选）：** 顺带改的关联文件。

### 分组方式

按优先级分组：
- **关键缺陷（P0）**：功能不可用 / 数据错误 / 安全性
- **健壮性提升（P1）**：容错、边界处理、一致性
- **体验优化（P2）**：UI、导出、文档
- **打磨（P3）**：注释、版本号、README 同步

### 队友改动说明

队友的改动需要单独加一节说明，标明改动来源和本版本在其基础上的增强。

### 版本规划表

每次新增版本后在版本规划表中同步更新状态（从「规划中」改为「已完成」）。

### 禁止行为

- 不得将详细版替换为简略版（如只写要点列表、没有代码对照）
- 不得覆盖或删除旧版本的详细内容
- 不得只写「做了什么」而不写「为什么」和「好在哪里」

---

## 前端 JS 修改强制规则（2026-07-16 新增）

**修改 `app/web/templates/index.html` 后必须立刻执行以下验证，缺一不可：**

1. **语法检查**：
   ```
   node --check app/web/static/app.js
   node --check app/web/static/participants.js
   ```

2. **字符串拼接排查**：凡是在 JS 中构造 HTML 字符串的地方，必须确认变量的拼接符号 `+` 没有被写入字符串字面量内部。常见错误模式：
   ```js
   // ❌ 错误：变量被写进了字符串里
   '+t.end_date.slice(5,10)(5)+'
   
   // ✅ 正确：变量在字符串外面用 + 拼接
   +' ~ '+t.end_date.slice(5,10)+'
   ```

3. **Unicode 转义排查**：JS 字符串中不能使用 `\u0022` 表示双引号，必须用 `\"`。`\uXXXX` 只在 HTML 层面有效，在 JS 源码中会被提前解析破坏字符串边界。

4. **运行全部测试**：`python -m pytest tests/ -q`，必须 401 passed。
   conftest 已为 legacy 模式打桩 `LLM_API_KEY`，无需配置任何密钥即可复现。

**以上 4 步全部通过才能提交。改完不验证就提交 = 极大概率引入 404 或页面无响应 bug。**
