# 昇腾挑战赛全量备赛手册

> 更新时间：2026-08-22
> 用途：新对话/新成员接手时的唯一入口。先读本手册，再按需翻后面的专项文档。
> 目录：`competition-ascend`（昇腾版工作区，即交付仓库 `sytweijin/competition` 的 main，
> 比赛期间仓库保持私有、结束后再公开）；桌面上的 `competition` 是基础版 v5.x 存档，
> **切勿从该目录 push**，以免与昇腾交付版冲突。

> **接手 P2 先读**：[交接-P2起点.md](./交接-P2起点.md)（2026-08-22 长对话记忆固化：已完成清单、架构决策、红线、P2 待办）。

## 1. 一句话现状

参赛赛道是**创新应用赛道**，项目是“团队协作分工智能体”。**P0 与 P1 已全部完成**（前端 Realtime 对话/语音、语音拍照需求输入、答辩直接语音视频对话、会议旁听、成员轻量汇报页、群通知、今日播报），**P2 视频理解 / 移动端适配 / 演示脚本已完成（v7.0）**，全量测试 401 passed。昇腾 A3 已成功部署 `llama.cpp-omni + F16`，本地文本/音频/视觉推理验证通过；**v7.1 完成全链路审计修复**：A3 长音频防崩与长会议智能分片（3.4 分钟会议实测可用）、语音记忆与转写质量闸门、中文导出编码、崩溃守护。当前 `.env` 为本地 A3 模式（`ASCEND_OMNI_WS_URL` 已启用），演示/录屏即用；长会议如遇模型偶发不稳可临时注释该行切换云端。剩余重点是 P2 比赛材料：PPT、演示视频录屏、公网 Demo。

## 2. 赛事信息

| 项目 | 内容 |
|------|------|
| 赛事 | OpenBMB / 面壁智能 × 华为昇腾全模态模型挑战赛 |
| 主题 | 围绕 `MiniCPM-o 4.5` 全模态模型，在昇腾 NPU 上做推理与应用落地 |
| 赛道 | 赛道一高性能推理优化（llama.cpp-omni / vLLM-Omni 两个子赛道）；**赛道二创新应用** |
| 截止 | 官方口径 2026-08-31 |
| 评审重点 | 创新性、实用性、场景价值或趣味性；工程完整度；可运行可展示；统一昇腾环境复现 |
| 应用赛道模型 | `MiniCPM-o-4.5-Realtime`（官方免费 API）；本地推荐 `MiniCPM-o-4_5-F16.gguf + llama.cpp-omni` |
| 算力 | A3 = 910C，A2 = 910B；A3 难抢，关机后难开；多卡环境卡时按倍率消耗 |

官方/重要链接：

- 赛事官网：https://ascend.openbmb.cn/competition
- 微信推文：https://mp.weixin.qq.com/s/k3wJDodQwqrdOq2zu1pmog
- Realtime API 文档：https://platform.modelbest.cn/console/docs/api/realtime
- MiniCPM-o Realtime 文档：https://minicpmo45.modelbest.cn/docs/zh/realtime-api/overview/
- 飞书 wiki（需要登录）：算力申请、答疑群、部署指南

## 3. 项目现状

项目定位：把项目要求、成员能力、截止日期转化为可编辑的任务拆解、分工、排期和报告，并在此基础上叠加 MiniCPM-o 全模态交互。

已接入能力（详见 [交接-P2起点.md](./交接-P2起点.md)）：

- 文本/语音对话（带当前方案快照）、图片理解（含非文字内容）、音频转写
- 答辩直接语音/视频对话（评委点评追问 + 表现观察）、会议旁听
- 成员轻量汇报页（token 链接、语音汇报、拍照交付、负责人确认完成）
- 群通知自动推送、今日播报（云端 TTS）
- TTS 文本朗读（仅云端）；本地昇腾后端：配置 `ASCEND_OMNI_WS_URL` 后走 A3

关键文件：

| 文件 | 说明 |
|------|------|
| `app/services/realtime_client.py` | Realtime 协议客户端，支持云端 MAP 与本地 llama-omni 两种模式 |
| `app/web/routers/realtime.py` | `/api/realtime/status`、`/api/realtime/chat` |
| `app/services/media_analysis.py` | 图片理解 / 音频转写，优先走 Realtime，旧模型回退 |
| `app/config.py` | `MAP_REALTIME_*`、`ASCEND_OMNI_*` 等配置 |
| `scripts/ascend_*.py/.sh` | A3 模型下载、启动服务、冒烟测试脚本 |
| `docs/昇腾A3_910C_llama_omni部署指南.md` | A3 完整部署手册 |
| `docs/华为昇腾创新应用赛道接入说明.md` | API 与本地后端接入说明 |
| `docs/演示脚本.md` | 五步多模态演示脚本（拍照→说话→确认→汇报→群通知） |
| `docs/项目说明.md` / `docs/复现文档.md` | 提交用项目说明与复现文档 |
| `docs/群聊记录/` | 原始群聊记录 + 经验索引 |

## 4. 技术架构与配置

### 4.1 两种后端

| 后端 | 配置 | 说明 |
|------|------|------|
| `map` | `MAP_REALTIME_API_KEY=...` | ModelBest 云端 Realtime API，免费，开发期兜底 |
| `local` | `ASCEND_OMNI_WS_URL=ws://127.0.0.1:28099/backend` | 昇腾 A3 本地 llama-omni-server，评审期主力 |

`/api/realtime/status` 返回 `backend: "local" | "map"`，可直观确认当前链路。

### 4.2 环境变量

```dotenv
MAP_REALTIME_API_KEY=你的Key
MAP_REALTIME_MODEL=MiniCPM-o-4.5-Realtime
MAP_REALTIME_BASE_URL=wss://api.modelbest.cn/v1/realtime
MAP_REALTIME_MAX_TOKENS=1024
MAP_REALTIME_TIMEOUT=60

ASCEND_OMNI_WS_URL=ws://127.0.0.1:28099/backend
ASCEND_OMNI_TIMEOUT=300
```

`MAP_REALTIME_API_KEY` 只放 `.env`，绝不提交 Git。

### 4.3 本地开发流程

本地跑应用：

```bash
python -m app.main
```

检查后端：

```bash
Invoke-RestMethod -Uri http://127.0.0.1:8000/api/realtime/status
```

测试对话：

```powershell
$body = @{ message = "你好" } | ConvertTo-Json
Invoke-RestMethod -Uri "http://127.0.0.1:8000/api/realtime/chat" -Method Post -ContentType "application/json" -Body $body
```

### 4.4 昇腾端口转发

本地应用要连 A3，需要在 VSCode Remote-SSH 的 Ports 面板把 A3 的 `28099` 转发到本地。关掉转发或 A3 关机，本地 `local` 链路会失败；纯业务开发时可临时注释 `ASCEND_OMNI_WS_URL` 切回云端 API。

## 5. 昇腾部署进度

已完成：

- A3 环境：CANN 9.1.0-beta.1、Ascend 910C、Python 3.12.13
- 模型：`/workspace/MiniCPM-o-4_5-gguf/` 10 个 GGUF 文件已下载，F16 主模型约 16G
- 源码：`llama.cpp-omni` 已 clone（国内走 `ghfast.top` / `gh-proxy.com` 等代理）
- 编译：`-DGGML_CANN=ON -DLLAMA_OPENSSL=OFF` 成功，含 `llama-omni-server` / `llama-omni-cli`
- 服务：`llama-omni-server` 监听 `0.0.0.0:28099`，`/health` 返回 `{"engine":"comni","status":"ok"}`
- 验证：文本 7.94 秒出回复；音频 2.86 秒理解并回答

关键命令（A3 上）：

```bash
cd /workspace/llama.cpp-omni
nohup ./ascend_start_server.sh > /tmp/llama-omni-server.log 2>&1 &
curl http://127.0.0.1:28099/health
```

已知风险：

- A3 资源紧张，关机后可能很难再开；多卡环境卡时翻倍
- 910C + llama.cpp-omni 的 TTS 有已知 `aclnn_repeat_interleave` 算子报错；官方分支应可用，报错找官方
- `vision_backend` 默认 `metal`，Linux/昇腾上视觉输入仍需适配
- `llama-omni-server` 只支持一个活跃 session，WebSocket 与 HTTP 不要混用
- 应用侧用 `websockets` 并已对本地后端禁用 keepalive ping；独立冒烟脚本用 `websocket-client`（详见 A3 指南 §7.2）

## 6. 群聊经验（已归档）

原始记录：

- `docs/群聊记录/2026-08-04_至_08-14_群聊记录.md`
- `docs/群聊记录/2026-08-15_至_08-20_群聊记录.md`
- `docs/群聊记录/README.md`

核心结论：

1. 创新应用赛道指定 `llama.cpp-omni`；`vLLM-Omni` 是推理优化赛道子赛道 B
2. 昇腾镜像选 CANN；910C 上 F16 最快，Q4_K_M 会退 CPU
3. A3 难抢、关机后难开；卡时不足可申请补充
4. SSH 不稳时优先 WebIDE；频繁 SSH 重连可能被封 IP（418）
5. TTS 有已知算子问题，官方分支应可用
6. 视觉后端 Linux/昇腾未完全适配
7. 官方免费 API 和本地部署官方都接受
8. 截止 8 月 31 日
9. Demo 公网访问：WebIDE 端口转发会随关闭失效；Dev Tunnel / SSH 隧道 / VPS 可常驻
10. MiniCPM-o 4.5 是 8B 模型，数学/强推理弱，演示避开高难度解题

## 7. 下一步任务清单（P2）

### P2：视频理解（重点）

- ✅ 已完成（v7.0）：录一段项目讨论/演示视频 → 抽帧 + 抽音频 → 边看边听总结任务、生成草案
- `/api/realtime/meeting` 录音/录像皆可；无声轨录屏仅凭画面整理；实测端到端 16s

### P2：移动端适配完善

- ✅ 已完成（v7.0）：汇报页已手机可用；工作台录音/拍照/摄像头在窄屏完成实测修补
  （390×844 无横向溢出、摄像头失败回退、0 控制台报错）

### P2：演示脚本改造

- ✅ 已完成（v7.0）：[演示脚本.md](./演示脚本.md) 五步多模态驱动：拍照 → 说话 → 确认 → 汇报 → 群通知

### P2：比赛材料

- ✅ 项目说明：[项目说明.md](./项目说明.md)（架构、模块、API、复现摘要、测试结果）
- ✅ 复现文档：[复现文档.md](./复现文档.md)（环境版本、模型、启动命令、测试结果、已知问题）
- ⏳ PPT：项目背景、场景、技术架构、创新点、全模态演示截图（待做）
- 演示视频：完整展示文本、图片、语音、TTS，最好在昇腾环境录屏
- 公网 Demo：WebIDE 转发临时可用；长期用 Dev Tunnel / SSH 隧道 / VPS

### 可选加分

- 云端 `mode=audio` 全双工实时对话
- 企业微信"应用消息"定向推送（需 corpid/secret）

## 8. 提交材料清单

1. 可运行 Demo / Web Demo / App（含源码、部署和启动说明）
2. PPT
3. 项目说明
4. 演示视频
5. 建议附：系统架构图、接口文档、健康检查、性能/延迟记录、故障兜底说明

## 9. 风险与兜底

| 风险 | 兜底 |
|------|------|
| A3 关机/排队拿不到 | 开发期用 MAP API 继续开发；最终用 API 录 Demo 并如实标注 |
| 910C TTS 算子报错 | 联系官方；演示一律用云端 API 做 TTS；本地后端严禁开 TTS（会挂起单会话服务、health 无响应，需重启 llama-omni-server） |
| 视觉后端昇腾不可用 | 先用 API 演示视频；后续适配 llama-omni vision |
| 端口转发失效 | 用 VSCode Ports / Dev Tunnel / SSH 隧道 / VPS |
| 磁盘被平台回收 | 优先用 `/workspace/shared_assets` 共享模型，别重复下载 |
| 卡时不足 | 申请补充；不用时关机；多卡环境注意倍率 |

## 10. 新对话接手指南

新对话开始时：

1. 先读本手册
2. 接 P2 时再读 `docs/交接-P2起点.md`
3. 按需读 `docs/昇腾A3_910C_llama_omni部署指南.md` 和 `docs/群聊记录/README.md`
4. 用户输入“1”表示继续完成任务
5. 所有昇腾赛代码改动只放 `competition-ascend`（昇腾版已推送为交付仓库 main，
   比赛期间私有）；`competition` 本地目录是基础版存档，禁止 push
6. 每次修改代码必须同步 `CHANGELOG.md`，按 AGENTS.md 格式写
7. 前端改动必须跑 `node --check app/web/static/app.js` 和 `participants.js`，再跑全量测试

## 11. 关键速查

```bash
# 本地起服务
python -m app.main

# 演示前一键预检（应用、A3、配置、MiniCPM-o 暖机）
python scripts/preflight_demo.py

# 查看当前后端
Invoke-RestMethod -Uri http://127.0.0.1:8000/api/realtime/status

# A3 启动 llama-omni-server
cd /workspace/llama.cpp-omni
nohup ./ascend_start_server.sh > /tmp/llama-omni-server.log 2>&1 &

# A3 健康检查
curl http://127.0.0.1:28099/health

# 全量测试
python -m pytest tests/ -q
```
